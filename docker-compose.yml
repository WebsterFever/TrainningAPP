services:
  postgres:
    image: postgres:16-alpine
    container_name: trainningWeb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: tatita12
      POSTGRES_DB: trainningdb
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
